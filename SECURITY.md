# 安全政策

## 支持范围

当前接收知客 `v2.1.x` 正式发行包的安全问题。第三方平台页面变化、账号风控和一般使用故障不属于安全漏洞，但可以按普通问题反馈。

## 如何报告

请通过[知客官网](https://zhike.crewup.cn/#buy)公布的联系邮箱私下报告。标题注明“知客安全问题”，并提供：

- 受影响版本；
- 可复现步骤和实际影响；
- 已完成的最小验证；
- 去除卡密、Cookie、登录态和个人信息后的证据。

不要在公开 Issue 中发布有效卡密、设备标识、Cookie、授权响应、签名链接、完整日志或用户数据。不要在未授权情况下测试他人的账号、设备或服务。

## 安全模型边界

- Web 服务只监听 `127.0.0.1`，并使用每次启动生成的本机会话凭据和写请求 CSRF 校验。
- 核心业务功能需要在线卡密；激活状态会在运行中定期复核。
- 平台登录态和业务数据默认保存在用户自己的电脑。
- 当前发行包的核心 Python 模块使用 PyArmor Basic 混淆，只用于提高直接复制和逆向分析成本；它不是加密或防本机管理员逆向的 DRM。能控制本机和修改程序文件的人仍可能绕过客户端限制，不承诺“无法破解”。
- 抖音的自动化许可、风控和账号处置由平台决定，软件护栏不能消除平台风险。
