# 更新记录

## v2.1.7 — 2026-08-09

- macOS Apple Silicon 与 Windows x64 核心 Python 模块采用 PyArmor Basic 混淆，提高直接复制和逆向分析成本，但不宣传“无法破解”。
- 两端制品统一绑定同一个已提交、干净的源码提交和受控构建证明，并保留最终点击前的在线授权复核。
- Windows 制品通过真实启动器、托管 Python 3.12.11、固定 Chromium、`/healthz`、错误卡、过期卡和会话重放防护验收。
- 官网与 GitHub Release 分别提供中文和 ASCII 文件名的同字节安装包，并使用同一 SHA-256 校验。
- 两端仍未使用商业代码签名，macOS 也未做 Apple 公证；首次打开边界与 Smart App Control 不兼容说明保持不变。

## v2.1.5 — 2026-08-09

- 第四步重组为运行控制、本次运行、系统状态与发送账本，待人工确认记录不会再因只展示最新 30 条而被隐藏。
- 全部界面图标改为随包内联 SVG，不依赖外部图标 CDN；补齐键盘导航、移动端布局和账本读取失败状态。
- macOS Apple Silicon 与 Windows x64 统一为 2.1.5，同一套在线卡密门和最终点击前授权检查。
- Windows 正式发行由固定依赖、托管 Python 3.12.11、固定 Chromium、真实启动器、`/healthz` 和无卡业务接口门禁共同验收。
- Windows SHA-256 校验文件固定使用 LF 行尾，官网包与 GitHub ASCII 包按原始字节复核一致。
- 两个平台当前均未使用商业代码签名，macOS 也未做 Apple 公证；首次安全确认步骤、Smart App Control 不兼容边界和 SHA-256 校验已写入说明。

## v2.1.2 — 2026-08-08

- 「立即自动跑一次」和「每小时自动巡查」统一为自动匹配并发送，不再要求逐批审核。
- 发送前强制复核卡密、账号、冻结批次和防重复账本；损坏账本会停止自动发送。
- 开关关闭、授权失效或发送状态未知时停止后续动作，不盲目重试。
- 激活前展示四点摘要和三份完整说明，用户主动确认后才提交卡密。
- 当前新用户正式交付平台为 macOS Apple Silicon；Windows v2.1.0 仅供已激活原设备恢复。
- 官网图片清晰度、响应式资源和 WebGL 帧率调度完成优化。

## v2.1.0 — 2026-08-01

- 将买家唯一支持入口统一为本机 Web App 与 `启动.command`。
- 所有业务 API 默认要求有效在线卡密，未激活仅保留诊断、激活和本机数据出口。
- 增加运行中授权复核、时间回拨检测及过期/吊销状态刷新。
- 发送前使用账号隔离、内容固定、15 分钟有效且只能使用一次的审核批次。
- 发送结果区分已确认、未知和失败；未知状态禁止盲目重试。
- 活跃时段与统计统一使用北京时间。
- 增加本机数据导出、恢复、清除和保留期规则。
- 固定 macOS Apple Silicon 依赖与发行清单；Windows 发行通道暂不开放。
